var React = require('react');
var createReactClass = require('create-react-class');

var Nav = createReactClass({
  render: function () {
    return (
      <h2>Nav component</h2>
    );
  }
});

module.exports = Nav;
